{
    "name": "studio-util",
    "version": "1.0.0",
    "author": "Virgil King <virgilking@gmail.com>",
    "type": "module",
    "main": "out/util.js",
    "scripts": {
        "build": "pnpm exec tsc",
        "clean": "rm -rf out/* tsconfig.tsbuildinfo",
        "test": "pnpm exec tsc -p tsconfig.test.json && pnpm exec vitest run --coverage --coverage.reporter=lcov out/*.test.js"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^1.5.2",
        "tsconfig": "workspace:*",
        "typescript": "^5.4.5",
        "vitest": "^1.5.2"
    }
}